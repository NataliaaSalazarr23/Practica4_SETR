#include "app_context.hpp"
namespace App
{
    // Estructura que almacena los handles de todas las tareas
    Handles g_handles;
    // Estructura que almacena los handles de las colas del sistema
    Queues g_queues;
}
